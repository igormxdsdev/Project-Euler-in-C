#include <stdio.h>
int main(){
    long long a = 1, b = 2, prox, soma = 0;
    while(b <= 4000000){
        if(b % 2 == 0){
            soma += b;
        }
        prox = a + b;
        a = b;
        b = prox;
    }
    printf("%lld\n", soma);
    return 0;
}

