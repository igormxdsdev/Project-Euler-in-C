

#include <stdio.h>
int main(){
    int x, soma;
    soma = 0;
    for(x = 0; x < 1000; x++){
        if(x % 3 == 0 || x % 5 == 0){
            soma += x;
        }
    }
    printf("%d\n", soma);
    return 0;
}
//233168
