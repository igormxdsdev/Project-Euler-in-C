#include <stdio.h>
int isPrime(long long n) {
    if (n < 2) return 0;
    for (long long i = 2; i * i <= n; i++) {
        if (n % i == 0) return 0; 
    }
    return 1;
}

int main(){

    long long a = 600851475143;
    long long b;
    long long n = 1;
    for(n=1; n<=775146; n++){
      if(a%n ==0){
         if(isPrime(n)){
            b = n;
            printf("%lld\n", b);
         }
      
      }
    } 
    
    return 0;
}

